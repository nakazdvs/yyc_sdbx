{
  "name": "new_sass",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "scss:css": "node-sass --output-style expanded ./scss -o ./css",
    "prefix": "postcss -u autoprefixer --no-map ./css -r ./css",
    "build": "yarn run scss:css && yarn run prefix"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
  },
  "devDependencies": {
    "node-sass": "^4.13.1",
    "autoprefixer": "^8.5.1",
    "postcss-cli": "^5.0.0"
  },
  "browserslist": [
    "> 1% in JP",
    "Android >= 4"
  ]
}
